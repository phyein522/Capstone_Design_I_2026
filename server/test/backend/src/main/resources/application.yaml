spring:
  application:
    name: backend
  cloud:
    openfeign:
      client:
        config:
          pythonClient: # @FeignClient? ?? name
            connectTimeout: 5000  # ?? ?? ?? (5?)
            readTimeout: 30000     # ?? ?? ?? (30?)